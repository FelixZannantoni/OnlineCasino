import { Game } from "./game";

export class slotmachine extends Game {

    constructor(gameId: string) {
        super(gameId);
    }

    public startGame() {

    }

    public nextRound() {
        this.spin();
    }

    public startRound() {
        if
    }

    private spin() {

    }
}